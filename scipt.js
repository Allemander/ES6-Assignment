class Employee {
    constructor(name, IdNumber, Permissions, StoreNumber) {
      this.name = name;
      this.IdNumber = IdNumber;
      this.Permissions = Permissions;
      this.StoreNumber = StoreNumber;
    }
  }

  class Manager {
    constructor(name, IdNumber, Permissions, StoreNumber, EmployeeNames, EmployeePermissions) {
      this.name = name;
      this.IdNumber = IdNumber;
      this.Permissions = Permissions;
      this.StoreNumber = StoreNumber;
      this.EmployeeNames = EmployeeNames;
      this.EmployeePermissions = EmployeePermissions;
    }
  }

  const countries = ['Finland', 'Estonia', 'Sweden', 'Denmark', 'Norway']
    let Finland = 'fin';
    let Estonia = 'est';
    let Sweden = 'swe';
    let Denmark = 'den';
    let Norway = 'nor';

    
    const rectangle = { 
      width: 20, 
      height: 10, 
      area: 200, 
      perimeter: 60 
      } 
      
    const rectangle = [20, 10, 200, 60];
    console.log(width)
    console.log(height)
    console.log(area)
    console.log(perimeter)


    const numbersEven = [0, 2, 4, 6, 10];
console.log('numbersEven: ', numbersEven);
    const numbersOdd = [1, 3, 5, 7, 9];
console.log('numbersOdd: ', numbersOdd);
const numbersCombined = [numbersEven, numbersOdd];
console.log('numbersCombined: ', numbersCombined);
numbersEven.map((numbersEven) => {
  return material.length;
});